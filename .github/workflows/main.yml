name: CI smoke

on:
  push:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Say hello
        run: echo "Workflow syntax is good ✅"
